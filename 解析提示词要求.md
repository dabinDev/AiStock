# Prompts

这组提示词专门给你的股票复盘软件使用，按三个时段拆分：

1. [开盘盘前解析-system-prompt-完整版.md](/E:/Github/taoguba/prompts/开盘盘前解析-system-prompt-完整版.md)
2. [盘中预期解析-system-prompt-完整版.md](/E:/Github/taoguba/prompts/盘中预期解析-system-prompt-完整版.md)
3. [盘后全量复盘-system-prompt-完整版.md](/E:/Github/taoguba/prompts/盘后全量复盘-system-prompt-完整版.md)

旧版 `*-system-prompt.md` 仍保留，但当前推荐接入的是 `*-system-prompt-完整版.md`。

## 设计原则

- 三份提示词都基于同一套交易体系：
  `周期 -> 节点 -> 主线 -> 核心 -> 竞价 -> 执行 -> 风控`
- 但每个时段只允许使用该时段真实可见的数据，不允许越界硬判。
- 输出统一要求：
  - 简体中文
  - 第一人称
  - 直接、可执行、少空话
  - 先市场定性，再讲题材和个股
  - 先事实，再推演，再动作

## 时间标题格式

三套输出的第一行都必须先输出一个纯时间标题，不要加 `#`、不要加 `【】`、不要加解释文字。

- 盘前固定格式：
  `04-01 09:25`
- 盘中固定格式：
  `04-01 10:10`
  说明：这里的时间不是写死 `10:10`，而是使用本次盘中解析的实际分钟，例如 `04-01 10:37`
- 盘后固定格式：
  `04-01 15:00`

统一规则：

- 格式固定为 `MM-DD HH:MM`
- 使用半角冒号 `:`
- 时间标题单独占一行
- 第二行再开始进入正文小节，如 `【竞价总览】`、`【盘前预期验证】`、`【今日市场定性】`

## 推荐接入方式

每次调用时：

1. 先加载对应时段的 `system prompt`
2. 再把软件抓取的数据按固定字段注入 `user prompt`
3. 尽量保持字段名稳定，不要今天叫 `jingjia`，明天改成 `auction_data`

## 建议的用户输入格式

不同场景下，把抓到的数据原样放进以下字段：

### 1. 开盘盘前解析

```text
日期：

[jingjia]
...竞价封单原始数据...

[jjyd]
...竞价异动原始数据，含5日竞价、竞价抢筹...
```

期望输出首行示例：

```text
04-01 09:25
```

### 2. 盘中预期解析

```text
日期：
当前时间：

[jingjia]
...竞价封单原始数据...

[jjyd]
...竞价异动原始数据...

[jinji]
...涨停股票池晋级原始数据...

[yidong]
...异动播报原始数据...
```

期望输出首行示例：

```text
04-01 10:10
```

说明：盘中必须使用你程序当前触发时的真实分钟，不要固定写成 `10:10`。

### 3. 盘后全量复盘

```text
日期：

[jingjia]
...竞价封单...

[jjyd]
...竞价异动...

[zhishu]
...指数行情...

[ztzhibo]
...涨停实时直播...

[yidong]
...异动播报...

[ztguchi]
...涨停股池...

[chengjiao]
...成交额...

[riqi_fupan]
...日期复盘...

[bankuaistrength]
...板块轮动，前20天涨停强度...

[jinji]
...涨停股票池晋级...
```

期望输出首行示例：

```text
04-01 15:00
```

## 后续建议

如果你后面要把这三套提示词继续升级，我建议按这个顺序做：

1. 再补一层 `字段字典`
2. 再补一层 `输出结构校验`
3. 再补一层 `关键结论提取`
4. 再补一层 `历史回测验证`
